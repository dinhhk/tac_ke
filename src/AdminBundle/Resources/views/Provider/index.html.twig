{% extends 'layoutAdminDefault.html.twig' %}
{% block content_app_body %}
    <div class="app-body">
        <div class="magic-layout">
            <div id="panel" class="panel panel-default magic-element width-full">
                <div class="panel-heading">
                    <div class="panel-icon"><i class="icon ion-ios7-photos-outline"></i></div>
                    <div class="panel-actions">
                        <a role="button" data-refresh="#panel" title="refresh" class="btn btn-sm btn-icon">
                            <i class="icon ion-refresh"></i>
                        </a>
                        <a role="button" data-expand="#panel" title="expand" class="btn btn-sm btn-icon">
                            <i class="icon ion-arrow-resize"></i>
                        </a>
                        <a role="button" data-collapse="#panel" title="collapse" class="btn btn-sm btn-icon">
                            <i class="icon ion-chevron-down"></i>
                        </a>
                        <a role="button" data-close="#panel" title="close" class="btn btn-sm btn-icon">
                            <i class="icon ion-close-round"></i>
                        </a>
                    </div><!-- /panel-actions -->
                    <h3 class="panel-title">Provider list</h3>
                </div><!-- /panel-heading -->
                <div class="table-responsive">
                    <table class="table table-hover datatables">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Name</th>
                                <th>Email</th>
                                <th>Address</th>
                                <th>Phone</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% set count = 0 %}
                            {% for provider in providers %}
                                {% set count = count + 1 %}
                                <tr>
                                    <td>{{ count }}</td>
                                    <td><a href="{{ path('provider_show', { 'id': provider.id }) }}">{{ provider.name }}</a></td>
                                    <td>{{ provider.email }}</td>
                                    <td>{{ provider.address }}</td>
                                    <td>{{ provider.phoneNumber }}</td>
                                    <td>
                                        <div class="btn-group">
                                            <a class="btn btn-ion btn-primary" href="{{ path('provider_new') }}">Add</a>
                                            <a class="btn btn-ion btn-success" href="{{ path('provider_edit', { 'id': provider.id }) }}">Edit</a>
                                            <a href="{{ path('provider_delete', {'id': provider.id }) }}" onclick="confirm('Are you sure?');" class="btn btn-danger btn-ion">Delete</a>
                                        </div>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                    {% if(providers|length == 0) %}
                    <div class="row">
                        <div class="col-sm-12">
                            <a class="btn btn-primary" href="{{ path('provider_new') }}">Add New</a>
                        </div>
                    </div>
                    {% endif %}
                </div><!-- /.table-responsive -->
            </div><!-- /panel -->
        </div>
    </div><!-- /app body -->
{% endblock %}