{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typeproduct</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeProduct.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('type_product_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('type_product_edit', { 'id': typeProduct.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
